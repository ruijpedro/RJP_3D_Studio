import React from 'react'
import {clearRjpLocalData} from './projectMigration'
export default class ErrorBoundary extends React.Component<React.PropsWithChildren,{error:Error|null}>{state:{error:Error|null}={error:null};static getDerivedStateFromError(error:Error){return{error}}componentDidCatch(error:Error,info:React.ErrorInfo){console.error('[RJP 3D] erro de interface',error,info)}render(){if(!this.state.error)return this.props.children;return <main className="rjp-recovery"><div className="rjp-recovery-card"><b>RJP 3D Studio</b><h1>A interface encontrou dados incompatíveis.</h1><p>O projeto guardado de uma versão anterior pode conter elementos num formato antigo. A aplicação não vai ficar num ecrã vazio.</p><pre>{this.state.error.message}</pre><div><button onClick={()=>location.reload()}>Tentar novamente</button><button className="danger" onClick={()=>{if(confirm('Limpar apenas os dados locais do RJP 3D Studio neste browser?')){clearRjpLocalData();location.reload()}}}>Limpar dados locais e reiniciar</button></div></div></main>}}
